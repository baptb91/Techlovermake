services:
  - type: web
    name: instagram-reel-api
    env: node
    plan: free
    buildCommand: npm install
    startCommand: node api/server.js
    envVars:
      - key: NODE_ENV
        value: production
    packages:
      - ffmpeg
